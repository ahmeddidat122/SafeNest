from django.urls import path
from . import views

app_name = 'devices'

urlpatterns = [
    # Add device-related URLs here when views are available
    # path('', views.device_list, name='device_list'),
    # path('<int:device_id>/', views.device_detail, name='device_detail'),
]
