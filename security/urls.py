from django.urls import path
from . import views

app_name = 'security'

urlpatterns = [
    # Add security-related URLs here when views are available
    # path('', views.security_dashboard, name='security_dashboard'),
    # path('alerts/', views.security_alerts, name='security_alerts'),
]
